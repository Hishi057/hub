# Hishi057-notes

ポートフォリオを置いたり勉強した内容をまとめて後から見返すためのリポジトリ。

## 編集方法(自分用)

`npx quartz build --serve` でローカルサーバー起動

## プロンプト (tex文字起こし用)

画像の数式文字起こしして
番号を振っているので、以下の形式で出力して

①
AIによる実際のレンダリング

```txt
$$
tex 数式
$$
```

※ただし複数行の場合、以下のように出力して

```txt
$$
\begin(aligned)
tex 数式
&=…
\end(aligned)
$$
```